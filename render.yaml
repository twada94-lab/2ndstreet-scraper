services:
  - type: cron
    name: 2ndstreet-scraper
    env: python
    schedule: "*/15 * * * *"  # 15分おきに実行
    buildCommand: |
      apt-get update && apt-get install -y chromium chromium-driver
      pip install -r requirements.txt
    startCommand: python scraper_line.py
